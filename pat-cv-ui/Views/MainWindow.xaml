<Window x:Class="pat_cv_ui.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:core="clr-namespace:Core;assembly=Core"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:pat_cv_ui.ViewModels"
        Title="{Binding Title}"
        Width="1280"
        Height="820"
        MinWidth="1280"
        MinHeight="820"
        d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}"
        prism:ViewModelLocator.AutoWireViewModel="True"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        >
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Border>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition
                    Width="160"
                    MinWidth="160"
                    MaxWidth="160" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid Margin="12">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>
                <hc:SideMenu>
                    <hc:SideMenu.Resources>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontSize" Value="20" />
                        </Style>
                    </hc:SideMenu.Resources>

                    <hc:SideMenuItem Command="{Binding HomeCommand}" Header="HOME">
                        <TextBlock Text="Home"/>
                    </hc:SideMenuItem>
                    <hc:SideMenuItem Command="{Binding SettingsCommand}" Header="SETTINGS">
                        <TextBlock Text="Settings"/>
                    </hc:SideMenuItem>
                </hc:SideMenu>

            </Grid>
            <ContentControl
                Grid.Column="1"
                Margin="12"
                prism:RegionManager.RegionName="{x:Static core:RegionNames.ContentRegion}" />
        </Grid>
    </Border>
</Window>
